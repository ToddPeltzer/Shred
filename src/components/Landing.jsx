import React from 'react';

function Landing({ beach }) {

    
    
    if (!beach) {
        return null
    } else {
    return (
        <div>
            
        </div>
    );
    }
}

export default Landing;